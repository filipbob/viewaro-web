{
  "name": "viewaro-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "generate:fixtures": "node scripts/generate-review-demo.mjs && node scripts/generate-vlc-dash-demo.mjs && node scripts/generate-playback-matrix-demo.mjs",
    "predev": "npm run generate:fixtures",
    "dev": "next dev",
    "prebuild": "npm run generate:fixtures",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "validate:review-demo": "node scripts/validate-review-demo.mjs",
    "validate:vlc-dash-demo": "node scripts/validate-vlc-dash-demo.mjs",
    "validate:playback-matrix": "node scripts/validate-playback-matrix.mjs"
  },
  "dependencies": {
    "next": "16.3.0",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.3.0",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
